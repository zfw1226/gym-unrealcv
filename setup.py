from setuptools import setup

setup(name='gym_unrealcv',
      version='1.0.0',
      install_requires=['gym==0.10.9', 'matplotlib', 'numpy', 'unrealcv', 'wget', 'opencv-python'],  # And any other dependencies foo needs
)
